import 'package:flutter/material.dart';

class GetBooking extends StatelessWidget {
  const GetBooking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.white,
      ),
    );
  }
}
